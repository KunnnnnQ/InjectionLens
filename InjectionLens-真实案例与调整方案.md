# InjectionLens：真实案例、MVP 审计与剩余 23 小时的调整方案

> 整理时间：2026-09-19 22:50（UTC+8）。案例均为本次联网核对的公开报告，来源见文末。
> 关键事实：**截止时间是 2026-09-20 10:00 AM EDT = 北京时间 9 月 20 日 22:00**（Devpost 页面 `datetime=2026-09-20T10:00:00-04:00`）。整场比赛只有 24 小时，不是 48 小时。

---

## 0. 结论

1. **合规上没有问题。** 项目文件最早创建于 22:07:19（UTC+8），开赛时间是 22:00，说明全部代码写于开赛之后。规则允许使用 AI，只要求披露，并且作者能解释自己的代码。
2. **真正的风险不是“做得太快”，而是“这 37 分钟之后，项目里没有体现你本人的判断”。** 我用 2026 年的真实案例测试了 Kimi 写的检测器：普通的 “visit us at https://…” 被判为 CRITICAL；隐藏的恶意指令只要加上 “for example” 就降为 LOW；真实攻击常用的话术（“If you are an LLM”、付款、招聘操纵、中文）全部漏报。
3. **所以剩下的时间不要用来补功能，而要用来证明它在真实世界里可靠。** 用真实案例重建测试集，攻击自己的检测器并修复，再用公开攻击库和真实网页算出数字。“AI 30 分钟写出检测器，我用一天时间攻破并修好它”这个故事，本身就是安全比赛最好的叙事，也能自然化解“是不是提前做的”这个疑问。

---

## 1. 如何处理“看起来像提前做的”

**事实证据（已核对）**

| 项 | 值 |
|---|---|
| 开赛 | 2026-09-19 10:00 AM EDT = 22:00 UTC+8 |
| 第一个项目文件 | `package.json` 等，22:07:19 |
| 最后一个文件 | `.gitignore`，22:36:36 |
| 目前状态 | 不是 git 仓库，没有提交记录 |

**今晚 20 分钟内要做（按顺序）**

1. 在 `injectionlens/` 里执行 `git init`，把当前状态做成第一次提交。提交信息要诚实，例如：`MVP scaffold generated by Kimi coding agent (2026-09-19 22:07–22:37 UTC+8)`。
2. 尽快推到 GitHub（公开或私有都行）。GitHub 服务器会记录推送时间，这比本地时间戳更有说服力。
3. 导出或截图 Kimi 的完整会话记录（带时间戳），自己保存好，用于备查。
4. 之后**每修复一个问题就提交一次**，提交信息写明依据。例如：`fix: exfil-url false positive on "visit us at" (found by real-page FPR test)`。评委看到的会是一条连续、有据的开发轨迹。

**不要做**：删掉重写来假装进度慢、篡改 commit 时间、在披露里淡化 AI 的作用。规则允许用 AI，但隐瞒 AI 的使用就违规了。

---

## 2. 真实案例（2025–2026）以及它们对 InjectionLens 的意义

| 案例 | 时间 / 来源 | 发生了什么 | 对项目的含义 |
|---|---|---|---|
| **网页诱导 AI 代理** | 2026-03-03，Palo Alto Unit 42 | 在真实网页中发现隐藏指令，要求 AI 批准诈骗广告（单页 24 处注入）、通过 PayPal 或 Stripe 付款（含 $5,000）、删除数据库、泄露系统提示、把求职者评为 “extremely qualified”。报告称首个在野案例是 2025 年 12 月的 AI 广告审核绕过。 | **投递方式分布：可见明文 37.8%、HTML 属性 19.8%、CSS 隐藏 16.9%；85.2% 使用社会工程话术。**这说明只看“是否隐藏”不够，属性是主要盲区。 |
| **Web 规模测量** | 2026-04-23，Google 安全团队 | 每个 Common Crawl 快照有 20–30 亿页，经过“规则 → Gemini → 人工”三级筛选。结果分为 6 类：恶作剧、有益引导、SEO、阻止 AI、窃取数据、破坏。**2025 年 11 月到 2026 年 2 月，恶意类别相对增长了 32%**。目前的攻击复杂度仍然较低。 | 可作为“问题正在增长”的权威证据，也说明业界的做法同样是先用规则、再用模型判断。 |
| **10 个在线 payload** | 2026-04-22，Forcepoint X-Labs | 包括让 AI 编程工具执行 `rm -rf`、PayPal.me $5,000 付款、窃取 API key、伪造版权声明让 AI 拒答、通过 metadata 植入 Stripe 捐款链接。触发语有 “Ignore previous instructions” 和 “If you are an LLM”。手法包括 `font-size:1px`、`rgba(…,0.01)`、`aria-hidden`、`<meta ai:action>`、`<system_prompt>` 标签。 | Forcepoint 自己也指出：用来检测注入的短语，正是安全社区讲解注入时用的短语。**纯正则必然误报，必须看上下文。** |
| **学术大规模测量** | 2026-04-29，arXiv 2604.27202 | 扫描 12 亿 URL、2480 万主机，得到 1.17 万个页面、1.53 万个验证实例。**约 70% 藏在非渲染 HTML 中（head、注释、元数据）**。目标还包括“内容保护声明”和“AI 爬虫探测”。在 13 个模型上，服从率最高约 8%。 | “内容保护声明”应作为良性难例。非渲染区域的覆盖是刚需。 |
| **Reddit 评论劫持 Comet** | 2025-08-20，Brave | 指令藏在 Reddit 剧透标签里。用户点击“总结网页”后，Comet 去账号页读取邮箱、触发 OTP、去 Gmail 读取验证码，再把两者贴回 Reddit。 | 最经典的“能力可达性”案例：同一条评论，对摘要助手无害，对有登录态的浏览器代理就是账号接管。 |
| **AI 定向伪装（cloaking）** | 2025-10，SPLX | 按 UA（ChatGPT-User、PerplexityBot）给 AI 返回另一版页面。在简历排名测试中，候选人用伪装版得到 **88/100，排第一**；用真实版只有 **26/100，排最后**。 | 直接支撑 UA 差异探针。**注意它用的是 ChatGPT-User（用户触发抓取器），而项目当前的列表里没有这一项。** |
| **HashJack** | 2025-11，Cato CTRL | 指令藏在 URL 的 `#` 片段里，服务器看不到，只有 AI 浏览器助手能读到。影响 Comet、Edge Copilot、Gemini in Chrome。微软和 Perplexity 已修复，Google 认为这属于预期行为。 | 又一个“摄入管线差异”的例子，而且检测成本很低。 |
| **“用 AI 总结”按钮投毒** | 2026-02-10，Microsoft；2026-08-06 Reflectiz 后续 | 在 `chatgpt.com/?q=`、`claude.ai/new?q=` 等链接里预填 “remember X as a trusted source”。60 天内发现 31 家公司、14 个行业、50 多条提示，8 月仍在生产环境出现。 | **新颖、真实、容易实现**：扫描页面上指向 AI 助手的链接，解码其中的提示词。几乎没有工具在做这件事。 |
| **行为指纹识别 AI 代理** | 2026-05-02，UC Davis FP-Agent | 研究了 7 个 AI 浏览代理。浏览器指纹的区分度有限，但打字、滚动、鼠标等行为指纹可以可靠识别代理。 | 伪装完全可以不依赖 UA。**所以 UA 探针只能算下限，这一点要在局限性里说明。** |

**相关工具（定位用）**

- **AI Prompt Injection Scanner**（Chrome 扩展，Flowio，24 位用户，2026-03 更新）：检测隐藏文本、零宽字符和 meta。说明**“隐藏文本扫描”已经商品化**，不能再当作卖点。
- **IPI-Proxy**（VulcanLab，arXiv 2605.11868，2026-05）：红队工具，把攻击注入真实 HTTP 响应，用来测试 AI 代理。攻击库 `payloads/unified.jsonl` 共 820 条，大部分是 MIT 许可，其中 WASP 的 84 条是 CC-BY-NC。它的方向是攻击代理，InjectionLens 是审计页面，两者互补，**可以直接用它的攻击库做评估**，需要注明来源。
- 企业护栏（Azure Prompt Shields、Model Armor、Bedrock Guardrails、Check Point/Lakera、BrowseSafe）：对文本给出判定结果，但不回答“哪个管线在页面哪个节点读到了什么”。

---

## 3. 用真实案例审计当前 MVP（已实测）

> 复现方法：直接用 `node` 调用 `server/lib/risk.js` 的 `analyzeInstruction` / `computeImpact`；SSRF 部分只请求本机地址。

| # | 问题 | 实测结果 | 位置 | 修复方向 |
|---|---|---|---|---|
| 1 | **普通句子被判严重** | “For more information, visit our help center at https://…” → summary-only 为 HIGH，full-access 为 **CRITICAL**。“Send your feedback to support@example.com” → **CRITICAL** | `server/lib/risk.js:90`（exfil-url 正则） | 只有“面向 AI 的祈使句”、隐藏投递或保密指令同时出现时才升级，否则记为 info |
| 2 | **两个词绕过** | 同一条隐藏的窃取指令：原样为 CRITICAL；加上 “for example” 或 “such as”，或者域名里含 “attacker”，就降为 **LOW** | `risk.js:106`（DEFENSIVE_CONTEXT）+ `risk.js:70`（封顶） | “引用或教学”折扣只适用于**人眼可见且位于 code/blockquote/引号内**的内容；隐藏内容和 AI 专属内容一律不打折 |
| 3 | **真实话术漏报** | “If you are an LLM…”、PayPal 付款、“extremely qualified”、“SYSTEM UPDATE…”、中文 payload 全部未检出；`rm -rf` 只被误归为 impersonation | `risk.js` INSTRUCTION_PATTERNS | 按 Unit 42 和 Google 的意图分类补充：面向 AI 的称呼、权威伪装、交易付款、破坏命令、推荐/审核/招聘操纵、伪造系统标签（`<system_prompt>`），再加中文规则 |
| 4 | **覆盖盲区** | HTTP 源管线只读元素文本、注释和 meta。不读属性（真实案例中占 19.8%）、JSON-LD、`<title>`、`noscript/template`、SVG | `profiles.js:41`、`:76` | 增加属性和结构化数据的提取 |
| 5 | **近似隐藏漏检** | 只判断 `opacity==0`、`font-size==0` 和颜色完全相同。Forcepoint 的真实样本用的是 `font-size:1px`、`rgba(…,0.01)` | `ingest.js` 渲染取证段 | 改用阈值：字号小于 4px、alpha 小于 0.1、对比度小于 1.2 |
| 6 | **定时注入漏检** | DOMContentLoaded 后只等 600ms 就截取快照；Unit 42 记录了“延时出现”的手法 | `ingest.js:32-33` | 在 +0.6s 和 +5s 各截一次快照并做 diff |
| 7 | **不可见字符不全** | 只覆盖 U+200B–200D、FEFF、2060；缺少双向控制字符（U+202A–202E、U+2066–2069）和 Unicode tag 字符（U+E0000–E007F） | `profiles.js:7`、`ingest.js:38` | 扩充字符集，匹配前先做 NFKC 归一化 |
| 8 | **UA 列表过时** | 只有训练爬虫（GPTBot/ClaudeBot/PerplexityBot），缺少用户触发的抓取器 ChatGPT-User、OAI-SearchBot、Claude-User、Perplexity-User；探针只试了前 2 个 | `ingest.js:7`、`analyze.js:96` | 补全列表并全部探测。UA 名称以各厂商官方文档为准 |
| 9 | **事实错误** | 测试服务器的正则里写了 `Google-Extended`，但它只是 robots.txt 令牌，**并不是 HTTP UA** | `server/index.js:35` | 删掉这一项，否则懂行的评委一问就会露馅 |
| 10 | **SSRF 防护可绕过** | `127.0.0.2`、`localhost.`、`[::ffff:127.0.0.1]`、`0.0.0.0` 都通过了检查（已实测）。另外，从代码看，重定向和浏览器子资源请求都没有校验 | `server/index.js:17` 起；`ingest.js:16` | **Demo 里不要宣称“有 SSRF 防护”。**要么限制为只能分析测试页和白名单，要么在 DNS 解析后校验 IP 并逐跳检查重定向 |
| 11 | **测试集是循环论证** | 7 个测试页和检测器是同一个 AI 在同一段 30 分钟里写的，“全部通过”几乎证明不了什么 | `server/fixtures/` | 用第 4 节的真实案例复刻集和外部攻击库重新评估 |

第 1、2 条最适合放进视频：**“我攻击了我自己的 AI 生成检测器，两个词就让它把严重攻击判成低风险。”**

---

## 4. 打磨后的 idea

**一句话（英文版可直接用）**

> Attackers have started writing to AI agents inside ordinary web pages. InjectionLens shows exactly what each kind of agent would ingest from a page — including what humans can't see — and which of those instructions could reach real damage.

**问题陈述要从“可能发生”改为“已经发生”**：Unit 42、Google、Forcepoint 和学术界在 2026 年都在真实网页里找到了针对 AI 的指令，而且恶意类别在三个月里相对增长了 32%。

**核心判断标准调整为**：这段文字是否在对 AI 说话、意图是什么、目标代理能否执行它。“隐藏”只作为加分信号，不作为门槛（37.8% 的真实 payload 是可见明文）。

**目标用户，建议选一个讲透**：让 AI 替自己阅读不可信网页并**据此做决定**的团队，比如用 AI 审核广告或内容的 Trust & Safety 团队、用 AI 筛简历的招聘方，以及浏览器代理的开发者。

**建议新增两种能力模板**：
- `decision agent`（广告审核或简历筛选）：没有工具，但**输出本身就是能力**。“批准诈骗广告”“把某人排第一”就是实际损害。这是现有三种模板都没覆盖的洞见。
- `coding agent (shell)`：对应 Forcepoint 发现的 `rm -rf` 类 payload。

**Demo 主线**：复刻 Unit 42 的诈骗广告落地页（人看是正常广告，页面里却给 AI 审核员留了多处“请批准”的指令，分布在可见页脚、data-* 属性、CSS 隐藏元素、注释和 JSON-LD 中），逐一展示哪条管线读到了哪一处，以及切换到 `decision agent` 后影响等级的变化。这个故事同时覆盖比赛列出的“AI 驱动安全”和“钓鱼与诈骗检测”两个方向，也和文件夹名 ScamLens 呼应：*Scammers now write to your AI.*

**差异化的说法**：隐藏文本扫描已经商品化（有现成的 Chrome 扩展）；护栏 API 只给出判定，不告诉你哪条管线、哪个节点；IPI-Proxy 是攻击代理的工具，我们用它的攻击库来评估；Web 规模研究属于遥测，不是团队可以直接拿来检查页面的工具。

**局限要主动讲**：UA 探针只是下限，行为指纹伪装抓不到；不覆盖截图和 OCR 类代理；没有任何扫描器能“解决”提示注入，InjectionLens 能做的是降低风险、让风险可见。

---

## 5. 剩余时间计划（现在 → 9/20 22:00 截止，21:00 前提交）

| 时间（UTC+8） | 做什么 | 产出 |
|---|---|---|
| 22:50–23:10 | 建立证据链（第 1 节） | 第一次 commit、GitHub、Kimi 会话记录 |
| 23:10–01:30 | **P0 修复**：第 3 节的 1、2、3、4、5、7、8、9；把本文的对抗样例写成回归测试；新增 8 个真实案例复刻页（广告审核、$5,000 PayPal、`rm -rf`、招聘离屏、Reddit 剧透、ChatGPT-User 伪装、“用 AI 总结”链接、内容保护声明作为良性难例），每页注明对应的报告出处 | 修复后的 commit 序列 |
| 01:30–08:30 | **睡觉**（视频质量取决于明天的状态） | — |
| 08:30–12:00 | **评估**：①IPI-Proxy 的 MIT 子集，抽约 50 条 × 3 种嵌入方式 × 6 个位置，得到检出率热力图（位置 × 管线）；②30–50 个真实良性网页，算修复前后的误报率；③复刻集结果表 | 3 组数字 + 1 张图 |
| 12:00–13:30 | **P1（只选一个）**：“用 AI 总结”链接审计 + URL 片段检查（推荐），或双时点快照 | 一个新颖功能 |
| 13:30–17:00 | 按 Demo 主线打磨 UI；写 README 和 Devpost 文案 | 可提交的页面 |
| 17:00–20:00 | 写视频脚本并录制（5 分钟以内） | 视频 |
| 20:00–21:00 | 提交 | — |
| 21:00–22:00 | 缓冲时间 | — |

**砍掉的内容**：PDF、截图和 OCR、公网部署（没有可靠的 SSRF 防护就不要部署；GitHub 仓库加视频就满足提交要求）、训练模型、行为伪装检测。LLM 二级判定是可选项：如果明天上午评估完还有时间、API 也可用，可以作为 P2，并按“检测器本身也是攻击面”的思路加固，即不给工具、内容加引号包裹、只输出 JSON。

**视频结构（约 4 分 50 秒）**：真实案例和数字（0:40）→ 为什么人眼查不出来（0:20）→ 诈骗广告 Demo：矩阵、能力切换、UA 探针、链接审计（2:00）→ 攻击自己的检测器，展示修复和评估数字（0:50）→ 用户、影响、局限（0:40）→ 技术栈和 AI 披露（0:20）。

---

## 6. AI 使用披露草稿（Devpost，按实际情况修改）

> **AI & External Tools Disclosure**
> - **ChatGPT** — before the 10:00 AM EDT start: brainstorming and background research on prompt-injection defenses (pre-event preparation, as permitted by the organizers). No code.
> - **Kimi coding agent** — in the first ~30 minutes after the start (22:07–22:37 UTC+8), generated the initial MVP from my pre-event plan: the four ingestion pipelines, the React UI, and the first seven synthetic test pages.
> - **Claude (Claude Code)** — researched 2025–2026 incident reports (Unit 42, Google, Forcepoint, Brave, SPLX, Microsoft) and ran adversarial tests against the AI-generated detector, surfacing false positives and a two-word bypass.
> - *[Fill in: which tools implemented the fixes and the evaluation, and which parts you designed or wrote yourself.]*
>
> I reviewed every change and can explain each module and design decision.

## 7. 评委可能的追问

- **“这不就是正则扫描器吗？”** 正则只是最便宜的第一层。价值在于：多条管线的来源对比、节点定位、能力可达性，以及真实数据上的评估。追问时可以直接展示局限性。
- **“和 Lakera / Model Armor 有什么区别？”** 它们对你交给它的文本给出判定；我们回答的是“哪种代理会在页面哪个位置读到什么，读到之后能造成什么后果”。
- **“30 分钟就做出来了？”** 是的，骨架由 AI 生成，这一点已披露。真正的工作是审计、修复和评估，可以展示 commit 历史。
- **“误报怎么样？”** 用第 5 节在真实网页上测出的误报率回答。
- **“攻击者能绕过吗？”** 能。视频里就展示了我们自己的绕过和修复。所以我们主张纵深防御，而不是把 InjectionLens 当作银弹。

---

## 来源

- Unit 42, *Fooling AI Agents: Web-Based Indirect Prompt Injection Observed in the Wild* (2026-03-03) — https://unit42.paloaltonetworks.com/ai-agent-prompt-injection/
- Google, *AI threats in the wild: The current state of prompt injections on the web* (2026-04-23) — https://blog.google/security/prompt-injections-web/
- Forcepoint X-Labs, *Indirect Prompt Injection in the Wild: X-Labs Finds 10 IPI Payloads* (2026-04-22) — https://www.forcepoint.com/blog/x-labs/indirect-prompt-injection-payloads
- Khodayari et al., *Indirect Prompt Injection in the Wild* (arXiv 2604.27202, 2026-04-29) — https://arxiv.org/abs/2604.27202
- Brave, *Agentic Browser Security: Indirect Prompt Injection in Perplexity Comet* (2025-08) — https://brave.com/blog/comet-prompt-injection/
- SPLX, *OpenAI's Atlas browser falls for AI-targeted cloaking attack* — https://splx.ai/blog/ai-targeted-cloaking-openai-atlas ；The Hacker News 报道（2025-10）— https://thehackernews.com/2025/10/new-ai-targeted-cloaking-attack-tricks.html
- Cato CTRL, *HashJack* (2025-11) — https://www.catonetworks.com/blog/cato-ctrl-hashjack-first-known-indirect-prompt-injection/
- Microsoft Security, *AI Recommendation Poisoning* (2026-02-10) — https://www.microsoft.com/en-us/security/blog/2026/02/10/ai-recommendation-poisoning/ ；Reflectiz 后续（2026-08-06）— https://thehackernews.com/2026/08/ai-recommendation-poisoning-how-ask-ai.html
- Wang, Shafiq, Vekaria, *FP-Agent: Fingerprinting AI Browsing Agents* (arXiv 2605.01247) — https://arxiv.org/abs/2605.01247
- *Manipulating LLM Web Agents with Indirect Prompt Injection Attack via HTML Accessibility Tree* (arXiv 2507.14799) — https://arxiv.org/pdf/2507.14799
- VulcanLab, *IPI-proxy* (arXiv 2605.11868) — https://arxiv.org/abs/2605.11868 ；仓库 https://github.com/VulcanLab/IPI-Proxy/
- Chrome 扩展 *AI Prompt Injection Scanner* — https://chromewebstore.google.com/detail/ai-prompt-injection-scann/hbiopoabiggfflofhfkpedppebcdncpi
- Google-Extended 不是 HTTP UA（Google 爬虫文档原文，转引）— https://knownagents.com/agents/google-extended
- 比赛页面与截止时间 — https://tln-cybersecurity-challenge.devpost.com/
